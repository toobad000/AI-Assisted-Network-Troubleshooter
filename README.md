# AI-Assisted-Network-Troubleshooter
Built a rule-based AI diagnostic engine integrated with GNS3 to automatically detect and resolve EIGRP and OSPF routing failures. The rule engine dynamically expands its knowledge base with each diagnostic run, improving fault identification and remediation accuracy over time as it builds context on the network topology.
